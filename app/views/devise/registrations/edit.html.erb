<div class="row">
  <div class="col-md-4" style="background-image: url(https://images.pexels.com/photos/698170/pexels-photo-698170.jpeg?auto=compress&cs=tinysrgb&dpr=2&h=650&w=940);"></div>
  
  <div class="col-md-5">
    
    
    <h2 style="font-weight: bold; font-size: 3em">Modifier mes infos personnelles</h2>

    <hr>
    <h5 style="font-weight: bold">Tous les champs sont obligatoires sauf ceux concernant la création d'un nouveau mot de passe</h5>
    <hr>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
    <%= render "devise/shared/error_messages", resource: resource %>

    <div class="field">
      <%= f.label :prenom, "Prénom :" %>
      <%= f.text_field :first_name, autofocus: true %>
    </div>

    <div class="field">
      <%= f.label :nom, "Nom :" %>
      <%= f.text_field :last_name, autofocus: true %>
    </div>

    <div class="field">
      <%= f.label :adresse_email, "Adresse e-mail :" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
    <% end %>

    <div class="field">
      <%= f.label :mot_de_passe_actuel, "Tapez ici votre mot de passe actuel :" %> 
      <%= f.password_field :current_password, autocomplete: "mot-de-passe-actuel" %>
    </div>

    <div class="field">
      <%= f.label :mot_de_passe, " Tapez votre nouveau mot de passe (minimum 6 caractères):" %>
      <%= f.password_field :password, autocomplete: "nouveau-mot-de-passe" %>
    </div>

    <div class="field">
      <%= f.label :confirmation, "Confirmation de votre nouveau mot de passe :" %>
      <%= f.password_field :password_confirmation, autocomplete: "nouveau-mot-de-passe" %>
    </div>

    <div class="actions">
      <%= f.submit "Valider" %>
    </div>
    <% end %>
    <hr>
    
    <h2 style="font-weight: bold;">Supprimer mon compte</h2>

    <p><strong>Attention :</strong> cette action est irréversible.<%= button_to "Je décide de supprimer mon compte", registration_path(resource_name), data: { confirm: "Etes-vous sûr?" }, method: :delete %></p>
  </div>
</div>
